export enum EavAttributeTypes {
    DATETIME = 'datetime',
    VARCHAR = 'varchar',
    TEXT = 'text',
    DECIMAL = 'decimal',
    INT = 'int',
}

export enum EavAttributeStatus {
    ENABLED = 'enabled',
    DISABLED = 'disabled',
}
